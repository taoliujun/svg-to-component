import type { FC } from 'react';
import { getColor } from '../utils/helper';
import type { SVGIconProps } from '../utils/types';

export const $componentName$: FC<SVGIconProps> = ({ color, ...props }) => {
    return $content$;
};
$componentName$.defaultProps = {
    color: $colors$,
};
