import type { FC } from 'react';
import { getColor } from '../utils/helper';
import type { SVGIconProps } from '../utils/types';

export const $componentName$: FC<SVGIconProps> = ({ color = $colors$, ...props }) => {
    return $content$;
};