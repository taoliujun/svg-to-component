import type { FC } from 'react';
import { getColor } from '$utilsPath$utils/helper';
import type { SVGIconProps } from '$utilsPath$utils/types';

export const $componentName$: FC<SVGIconProps> = ({ color = $colors$, ...props }) => {
    return $content$;
};
